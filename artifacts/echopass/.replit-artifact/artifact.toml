kind = "web"
previewPath = "/"
title = "EchoPass"
version = "1.0.0"
id = "artifacts/echopass"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24813

[services.development]
run = "pnpm --filter @workspace/echopass run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/echopass", "run", "build" ]
publicDir = "artifacts/echopass/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24813"
BASE_PATH = "/"
